---
slug: slos-and-error-budgets
month: 12
week: 3
ord: 3
title: "SLOs and Error Budgets"
code: "12.3.3"
minutes: 75
difficulty: 3
objectives:
  - "Convert an availability target into a downtime allowance"
  - "Use an error budget to decide whether to ship"
  - "Explain what each additional nine costs"
prereqs:
  - "Percentiles, Tails, and Why Averages Lie ✓"
sections:
  - { anchor: objectives, title: Objectives }
  - { anchor: intuition, title: Intuition }
  - { anchor: math, title: "An Availability Target Is a Time Budget" }
  - { anchor: python, title: Pure Python }
  - { anchor: quiz, title: Mini Quiz }
---

<h2 id="intuition">Turning 'should we ship?' into a lookup</h2>

Reliability arguments are usually unresolvable because both sides are describing feelings. An SLO converts the argument into arithmetic: here is the target, here is what we have spent, here is what remains.

<h2 id="math">An Availability Target Is a Time Budget</h2>

An availability target is a permitted downtime per period:

<MathBlock tex="\text{budget} = T \times (1 - \text{SLO})" />

Over 30 days: 99% is 432 minutes, 99.9% is **43.2 minutes**, 99.99% is 4.3 minutes. The **error budget** is what remains after subtracting what you have already spent — and it is a budget in the ordinary sense. Consistently *not* spending it means you are shipping too slowly, not that you are doing well.

<Callout kind="tip" title="Each nine costs an order of magnitude, not a bit more effort">
  99.99% allows **4.3 minutes a month** — less than a single deploy-and-rollback. Meeting it requires multi-region deployment, automated failover, and a much slower release cadence. That is not "try harder"; it is a different system, funded differently. Pick the target your users actually need: every extra nine is paid for in engineering time that is then unavailable for the product.
</Callout>

<h2 id="python">Availability targets, budgets, and what a restore costs against them</h2>

<CodeCell
  code={"\"\"\"Week 3: monitoring, logging, backups, and security in production.\"\"\"\nimport numpy as np\n\nrng = np.random.default_rng(0)\n\n# ---------------------------------------------------------------------------\n# FROM LOGS TO METRICS. Real access logs, parsed into the RED metrics.\n# ---------------------------------------------------------------------------\nLOG = \"\"\"\\\n127.0.0.1 - - [02/Jun/2024:09:31:04] \"GET /api/v1/lessons HTTP/1.1\" 200 8421 0.031\n127.0.0.1 - - [02/Jun/2024:09:31:04] \"GET /api/v1/health HTTP/1.1\" 200 17 0.001\n127.0.0.1 - - [02/Jun/2024:09:31:05] \"POST /api/v1/tutor/chat HTTP/1.1\" 200 2044 2.812\n127.0.0.1 - - [02/Jun/2024:09:31:05] \"GET /api/v1/lessons/lora HTTP/1.1\" 200 15302 0.044\n127.0.0.1 - - [02/Jun/2024:09:31:06] \"POST /api/v1/auth/login HTTP/1.1\" 401 62 0.118\n127.0.0.1 - - [02/Jun/2024:09:31:07] \"POST /api/v1/tutor/chat HTTP/1.1\" 500 41 0.902\n127.0.0.1 - - [02/Jun/2024:09:31:07] \"GET /api/v1/review/due HTTP/1.1\" 200 1180 0.067\n127.0.0.1 - - [02/Jun/2024:09:31:08] \"POST /api/v1/exercises/run HTTP/1.1\" 200 512 4.221\n127.0.0.1 - - [02/Jun/2024:09:31:08] \"GET /api/v1/lessons HTTP/1.1\" 200 8421 0.029\n127.0.0.1 - - [02/Jun/2024:09:31:09] \"POST /api/v1/tutor/chat HTTP/1.1\" 502 0 30.001\n\"\"\"\nimport re\nrows = []\nfor line in LOG.strip().splitlines():\n    m = re.search(r'\"(\\w+) (\\S+) [^\"]*\" (\\d{3}) (\\d+) ([\\d.]+)', line)\n    rows.append((m.group(1), m.group(2), int(m.group(3)), int(m.group(4)), float(m.group(5))))\n\nprint(\"STRUCTURED LOGS -> THE THREE METRICS THAT MATTER (RED)\")\nprint()\nn = len(rows)\nerrs = [r for r in rows if r[2] >= 500]\ndurs = sorted(r[4] for r in rows)\nprint(f\"  Rate     : {n} requests in the window\")\nprint(f\"  Errors   : {len(errs)} 5xx = {len(errs)/n:.1%}   (4xx excluded -- see below)\")\nprint(f\"  Duration : mean {np.mean(durs):.3f}s   p50 {np.percentile(durs,50):.3f}s   \"\n      f\"max {max(durs):.3f}s\")\nprint()\nprint(f\"  no p99 is reported, because there are only {n} requests here. A p99\")\nprint(f\"  needs ~100 samples before it means anything and ~1,000 before it is\")\nprint(\"  stable; computed on 10 points it just interpolates toward the max and\")\nprint(\"  reports it with false precision. Percentiles need volume, and a\")\nprint(\"  dashboard that shows p99 for a low-traffic endpoint is showing noise.\")\nprint()\nprint(\"  the 401 is NOT counted as an error. A rejected bad password is the\")\nprint(\"  system working; counting 4xx in your error rate means every password\")\nprint(\"  typo pages an engineer. 5xx is 'we broke'; 4xx is 'they asked wrongly'.\")\nprint()\nprint(\"  slowest endpoints (this is what a latency dashboard is FOR):\")\nby_ep = {}\nfor meth, path, st, sz, d in rows:\n    key = re.sub(r\"/lessons/[^/]+$\", \"/lessons/{slug}\", path)\n    by_ep.setdefault(key, []).append(d)\nfor ep, ds in sorted(by_ep.items(), key=lambda kv: -max(kv[1]))[:4]:\n    print(f\"    {ep:<28} n={len(ds)}  max {max(ds):>7.3f}s\")\nprint()\nprint(\"  note /lessons/lora was normalized to /lessons/{slug}. Without that,\")\nprint(\"  every lesson is its own metric series and the cardinality explodes --\")\nprint(\"  240 lessons x 5 status codes x 4 methods is 4,800 series for ONE route.\")\nprint(\"  High-cardinality labels are the most common way to take down a metrics\")\nprint(\"  backend, and user IDs in labels are the classic version of the mistake.\")\nprint()\n\n# ---------------------------------------------------------------------------\n# WHY AVERAGES LIE. This is the core observability result.\n# ---------------------------------------------------------------------------\nprint(\"WHY YOU NEVER ALERT ON AN AVERAGE\")\nprint()\n# realistic latency: mostly fast, with a heavy tail (cache misses, GC, retries)\nfast = rng.lognormal(np.log(0.030), 0.35, 95_000)\nslow = rng.lognormal(np.log(1.400), 0.80,  5_000)\nlat = np.concatenate([fast, slow])\nprint(f\"  100,000 requests: 95% served from cache, 5% miss and hit the model\")\nprint(f\"  {'mean':<10} {np.mean(lat):.3f}s\")\nfor p in [50, 90, 95, 99, 99.9]:\n    print(f\"  {'p'+str(p):<10} {np.percentile(lat, p):.3f}s\")\nprint()\nmean, p99 = np.mean(lat), np.percentile(lat, 99)\nworse = (lat > mean).mean()\nprint(f\"  the mean is {mean:.3f}s and p99 is {p99:.3f}s -- {p99/mean:.1f}x higher.\")\nprint(f\"  only {worse:.1%} of requests are slower than the mean, so an alert set\")\nprint(f\"  at 2x the mean fires on almost nothing while {(lat>1.0).mean():.1%} of users wait\")\nprint(\"  over a second. Averages describe a population no user belongs to.\")\nprint()\n\n# ---------------------------------------------------------------------------\n# TAIL AT SCALE. The result that justifies obsessing over p99.\n# ---------------------------------------------------------------------------\nprint(\"TAIL AT SCALE: WHY p99 IS A COMMON CASE, NOT AN EDGE CASE\")\nprint()\nprint(\"  one user action fans out to N backend calls. The page is slow if ANY\")\nprint(\"  of them is slow, so the probability of a slow page is 1 - (1-p)^N:\")\nprint(f\"  {'fan-out N':>10} {'P(slow page), p99 backend':>28}\")\nfor N in [1, 5, 10, 50, 100, 200]:\n    print(f\"  {N:>10} {1 - 0.99**N:>27.1%}\")\nprint()\nprint(\"  at a fan-out of 100 -- ordinary for a page assembling lessons, progress,\")\nprint(\"  reviews and recommendations -- a backend that is 'only' 1% slow makes\")\nprint(f\"  {1-0.99**100:.0%} of page loads slow. The rare event happens to the majority of\")\nprint(\"  users, because each user touches the service many times.\")\nprint(\"  This is why p99 is a product requirement and not a vanity metric.\")\nprint()\n\n# ---------------------------------------------------------------------------\n# SLOs AND ERROR BUDGETS.\n# ---------------------------------------------------------------------------\nprint(\"SLOs AND ERROR BUDGETS: PERMISSION TO SHIP\")\nprint()\nMIN_PER_30D = 30 * 24 * 60\nprint(f\"  {'availability':>14} {'downtime / 30 days':>22} {'per day':>12}\")\nfor slo in [0.99, 0.995, 0.999, 0.9999]:\n    down = MIN_PER_30D * (1 - slo)\n    print(f\"  {slo:>13.2%} {down:>19.1f} min {down/30:>9.1f} min\")\nprint()\nprint(\"  an error budget is the inverse of an SLO, and it is a BUDGET: at 99.9%\")\nprint(f\"  you may be down {MIN_PER_30D*0.001:.0f} minutes a month, and if you are not spending\")\nprint(\"  it you are shipping too slowly. Consumed budget is the objective\")\nprint(\"  trigger for freezing releases -- which turns 'should we ship?' from an\")\nprint(\"  argument into a lookup.\")\nprint()\nprint(\"  and the reason four nines is not simply 'better': it ALLOWS\")\nprint(f\"  {MIN_PER_30D*0.001/(MIN_PER_30D*0.0001):.0f}x less downtime than three -- {MIN_PER_30D*0.0001:.1f} minutes a month, less than a\")\nprint(\"  single deploy-and-rollback. Meeting it requires multi-region, automated\")\nprint(\"  failover, and a much slower release cadence. Pick the SLO\")\nprint(\"  your users actually need; every extra nine is paid for in engineering\")\nprint(\"  time that is then unavailable for features.\")\nprint()\n\n# ---------------------------------------------------------------------------\n# BACKUPS. The only test that counts is a restore.\n# ---------------------------------------------------------------------------\nprint(\"BACKUPS: RPO AND RTO ARE THE ONLY TWO NUMBERS\")\nprint()\nprint(\"  RPO -- how much data you may lose  (set by backup FREQUENCY)\")\nprint(\"  RTO -- how long recovery may take  (set by restore SPEED)\")\nprint()\nDB_GB, RESTORE_MB_S = 40, 80\nprint(f\"  {'strategy':<34} {'RPO':>10} {'RTO (40 GB db)':>16}\")\nrestore_s = DB_GB * 1024 / RESTORE_MB_S\nfor name, rpo_min, extra_s in [\n        (\"nightly dump only\", 24*60, 0),\n        (\"dump every 6h\", 6*60, 0),\n        (\"nightly + WAL archiving\", 5, 900),\n        (\"nightly + WAL + hot standby\", 5, 60)]:\n    rto = restore_s + extra_s\n    print(f\"  {name:<34} {rpo_min:>7.0f} min {rto/60:>13.1f} min\")\nprint()\nbudget_999 = MIN_PER_30D * 0.001\nprint(f\"  a {DB_GB} GB restore at {RESTORE_MB_S} MB/s takes {restore_s/60:.1f} minutes BEFORE any\")\nprint(\"  replay, DNS change, or cache warm-up. Against a 99.9% SLO's\")\nprint(f\"  {budget_999:.0f}-minute monthly budget, that bare restore is \"\n      f\"{restore_s/60/budget_999:.0%} of the month's\")\nprint(f\"  entire allowance -- and the WAL-archiving row, at {(restore_s+900)/60:.1f} minutes, is\")\nprint(f\"  {(restore_s+900)/60/budget_999:.0%} of it, i.e. one such event breaks the SLO on its own.\")\nprint(\"  Recovery time is not an ops detail; it is a direct claim on the same\")\nprint(\"  budget your releases are spending.\")\nprint()\nprint(\"  and the untested backup: a backup you have never restored is a\")\nprint(\"  HYPOTHESIS. The failure modes -- truncated dumps, missing extensions,\")\nprint(\"  a wrong encoding, an unreadable key -- are all invisible until the\")\nprint(\"  restore, which is the worst possible moment to discover them. Schedule\")\nprint(\"  restores, not just backups.\")\nprint()\n\n# ---------------------------------------------------------------------------\n# SECURITY: rate limiting and the shape of abuse.\n# ---------------------------------------------------------------------------\nprint(\"RATE LIMITING: A TOKEN BUCKET, AND WHY BURSTS ARE ALLOWED\")\nprint()\ndef bucket(events, rate, burst):\n    tokens, last, out = float(burst), 0.0, []\n    for t in events:\n        tokens = min(burst, tokens + (t - last) * rate)\n        last = t\n        if tokens >= 1:\n            tokens -= 1; out.append((t, True))\n        else:\n            out.append((t, False))\n    return out\n\nnormal = [0.0, 0.1, 0.2, 1.0, 2.0, 3.0, 5.0, 5.1]           # a person\nabusive = [i * 0.01 for i in range(60)]                      # a script\nfor label, ev in [(\"interactive user\", normal), (\"scripted abuse\", abusive)]:\n    res = bucket(ev, rate=2.0, burst=5)\n    ok = sum(1 for _, a in res if a)\n    print(f\"  {label:<18} {len(ev):>3} requests -> {ok:>3} allowed, \"\n          f\"{len(ev)-ok:>3} rejected ({(len(ev)-ok)/len(ev):.0%})\")\nprint()\nprint(\"  rate=2/s with burst=5 lets a real person click quickly without ever\")\nprint(\"  seeing a 429, while a script hammering at 100/s is cut off almost\")\nprint(\"  entirely. A flat cap tuned low enough to stop the script would break\")\nprint(\"  the human; the burst allowance is what separates them.\")\nprint()\nprint(\"  where to enforce it matters as much as the numbers: this belongs in\")\nprint(\"  nginx, at the edge, BEFORE the request reaches an application worker.\")\nprint(\"  A limiter inside the app has already spent the expensive resource --\")\nprint(\"  a worker process -- to decide not to serve the request.\")\n"}
  output={"STRUCTURED LOGS -> THE THREE METRICS THAT MATTER (RED)\n\n  Rate     : 10 requests in the window\n  Errors   : 2 5xx = 20.0%   (4xx excluded -- see below)\n  Duration : mean 3.823s   p50 0.092s   max 30.001s\n\n  no p99 is reported, because there are only 10 requests here. A p99\n  needs ~100 samples before it means anything and ~1,000 before it is\n  stable; computed on 10 points it just interpolates toward the max and\n  reports it with false precision. Percentiles need volume, and a\n  dashboard that shows p99 for a low-traffic endpoint is showing noise.\n\n  the 401 is NOT counted as an error. A rejected bad password is the\n  system working; counting 4xx in your error rate means every password\n  typo pages an engineer. 5xx is 'we broke'; 4xx is 'they asked wrongly'.\n\n  slowest endpoints (this is what a latency dashboard is FOR):\n    /api/v1/tutor/chat           n=3  max  30.001s\n    /api/v1/exercises/run        n=1  max   4.221s\n    /api/v1/auth/login           n=1  max   0.118s\n    /api/v1/review/due           n=1  max   0.067s\n\n  note /lessons/lora was normalized to /lessons/{slug}. Without that,\n  every lesson is its own metric series and the cardinality explodes --\n  240 lessons x 5 status codes x 4 methods is 4,800 series for ONE route.\n  High-cardinality labels are the most common way to take down a metrics\n  backend, and user IDs in labels are the classic version of the mistake.\n\nWHY YOU NEVER ALERT ON AN AVERAGE\n\n  100,000 requests: 95% served from cache, 5% miss and hit the model\n  mean       0.128s\n  p50        0.031s\n  p90        0.053s\n  p95        0.117s\n  p99        2.774s\n  p99.9      7.400s\n\n  the mean is 0.128s and p99 is 2.774s -- 21.7x higher.\n  only 5.0% of requests are slower than the mean, so an alert set\n  at 2x the mean fires on almost nothing while 3.3% of users wait\n  over a second. Averages describe a population no user belongs to.\n\nTAIL AT SCALE: WHY p99 IS A COMMON CASE, NOT AN EDGE CASE\n\n  one user action fans out to N backend calls. The page is slow if ANY\n  of them is slow, so the probability of a slow page is 1 - (1-p)^N:\n   fan-out N    P(slow page), p99 backend\n           1                        1.0%\n           5                        4.9%\n          10                        9.6%\n          50                       39.5%\n         100                       63.4%\n         200                       86.6%\n\n  at a fan-out of 100 -- ordinary for a page assembling lessons, progress,\n  reviews and recommendations -- a backend that is 'only' 1% slow makes\n  63% of page loads slow. The rare event happens to the majority of\n  users, because each user touches the service many times.\n  This is why p99 is a product requirement and not a vanity metric.\n\nSLOs AND ERROR BUDGETS: PERMISSION TO SHIP\n\n    availability     downtime / 30 days      per day\n         99.00%               432.0 min      14.4 min\n         99.50%               216.0 min       7.2 min\n         99.90%                43.2 min       1.4 min\n         99.99%                 4.3 min       0.1 min\n\n  an error budget is the inverse of an SLO, and it is a BUDGET: at 99.9%\n  you may be down 43 minutes a month, and if you are not spending\n  it you are shipping too slowly. Consumed budget is the objective\n  trigger for freezing releases -- which turns 'should we ship?' from an\n  argument into a lookup.\n\n  and the reason four nines is not simply 'better': it ALLOWS\n  10x less downtime than three -- 4.3 minutes a month, less than a\n  single deploy-and-rollback. Meeting it requires multi-region, automated\n  failover, and a much slower release cadence. Pick the SLO\n  your users actually need; every extra nine is paid for in engineering\n  time that is then unavailable for features.\n\nBACKUPS: RPO AND RTO ARE THE ONLY TWO NUMBERS\n\n  RPO -- how much data you may lose  (set by backup FREQUENCY)\n  RTO -- how long recovery may take  (set by restore SPEED)\n\n  strategy                                  RPO   RTO (40 GB db)\n  nightly dump only                     1440 min           8.5 min\n  dump every 6h                          360 min           8.5 min\n  nightly + WAL archiving                  5 min          23.5 min\n  nightly + WAL + hot standby              5 min           9.5 min\n\n  a 40 GB restore at 80 MB/s takes 8.5 minutes BEFORE any\n  replay, DNS change, or cache warm-up. Against a 99.9% SLO's\n  43-minute monthly budget, that bare restore is 20% of the month's\n  entire allowance -- and the WAL-archiving row, at 23.5 minutes, is\n  54% of it, i.e. one such event breaks the SLO on its own.\n  Recovery time is not an ops detail; it is a direct claim on the same\n  budget your releases are spending.\n\n  and the untested backup: a backup you have never restored is a\n  HYPOTHESIS. The failure modes -- truncated dumps, missing extensions,\n  a wrong encoding, an unreadable key -- are all invisible until the\n  restore, which is the worst possible moment to discover them. Schedule\n  restores, not just backups.\n\nRATE LIMITING: A TOKEN BUCKET, AND WHY BURSTS ARE ALLOWED\n\n  interactive user     8 requests ->   8 allowed,   0 rejected (0%)\n  scripted abuse      60 requests ->   6 allowed,  54 rejected (90%)\n\n  rate=2/s with burst=5 lets a real person click quickly without ever\n  seeing a 429, while a script hammering at 100/s is cut off almost\n  entirely. A flat cap tuned low enough to stop the script would break\n  the human; the burst allowance is what separates them.\n\n  where to enforce it matters as much as the numbers: this belongs in\n  nginx, at the edge, BEFORE the request reaches an application worker.\n  A limiter inside the app has already spent the expensive resource --\n  a worker process -- to decide not to serve the request."}
/>

<h2 id="quiz">Check yourself</h2>

<QuizCard
  question="A team has consumed 5% of its monthly error budget with three weeks remaining. What does that indicate?"
  correct={2}
  options={[
    { text: "The service is unusually reliable and the SLO should be tightened", why: "Possibly worth reviewing eventually, but a single period of underspend is weak evidence and tightening the SLO raises costs immediately." },
    { text: "Nothing -- error budgets are only meaningful when exhausted", why: "Underspend is informative in its own right, which is one of the more counter-intuitive aspects of running SLOs." },
    { text: "The team is likely being too conservative -- an unspent budget represents reliability bought at the cost of shipping speed, so it is a signal that they could deploy more often or take more risk", why: "Correct -- this is what makes an error budget a decision tool rather than a report card. It authorizes risk as well as constraining it." },
    { text: "The measurement window is too short to draw conclusions", why: "Three weeks of data against a monthly budget is a reasonable basis for adjusting release pace, even if not for permanently changing the SLO." },
  ]}
/>

## Summary

An availability target is a downtime allowance: 99.9% is **43.2 minutes** per 30 days, 99.99% is 4.3 — less than one deploy-and-rollback. The error budget makes shipping decisions mechanical, and it cuts both ways: an unspent budget means you are shipping too slowly. And recovery time competes for the same budget — a bare 40 GB restore consumes 20% of a month's allowance before any replay.

## Further reading

- Beyer et al., *Site Reliability Engineering*, chapters on SLOs and error budgets.
